description "application"
start on startup
stop on shutdown
respawn
chdir {{ home }}/application
exec {{ venv }}/bin/gunicorn -w {{ gunicorn_workers }} --log-level=warning --error-logfile=/tmp/error.log --user=nobody --group=nobody --bind=unix:/tmp/application.sock --limit-request-line=8190 runserver:app

